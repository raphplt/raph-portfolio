DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require
# Optional: DATABASE_URL is used to sign game sessions when this is omitted.
# CORE_RUNNER_SECRET=replace-with-a-random-secret-of-at-least-24-characters
